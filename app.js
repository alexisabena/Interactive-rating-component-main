const rating = document.getElementById('rating-form');
console.log(rating);

const ratings = document.getElementsByClassName('visible')
console.log(ratings);

const form = document.querySelector('form');
console.log(form);